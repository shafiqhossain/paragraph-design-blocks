{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}
{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    <div class="container clearfix">
      <div class="listing-wrapper">
        <div class="listing-section-left">
          {% if paragraph.field_section_title.value is not empty %}
            <h3 class="section-title">{{ paragraph.field_section_title.value }}</h3>
          {% endif %}
          {% if paragraph.field_sub_title.value is not empty %}
            <h4 class="sub-title">{{ paragraph.field_sub_title.value }}</h4>
          {% endif %}
          {% if paragraph.field_section_link.title is not empty %}
            {% set section_link_url = paragraph.field_section_link.0.url.toString() is empty ? '#' :
              paragraph.field_section_link.0.url  %}
            <a class="topic-block-link" href="{{ section_link_url }}">
              {{ paragraph.field_section_link.title }}
            </a>
          {% endif %}
          {% if content.field_anchor_menus is not empty %}
            <div class="topic-anchor-menus">{{ content.field_anchor_menus }}</div>
          {% endif %}
        </div>
        <div class="listing-section-middle">
          {% if paragraph.field_listing_block_style.value == 0 %}
            {% if listing_views is not empty %}
              <div class="listing-teasers-wrapper">
                {{ listing_views }}
              </div>
            {% endif %}
          {% else %}
            {% if listing_nodes|length > 0 %}
              <div class="listing-teasers-wrapper">
                {% if listing_nodes is not empty %}
                  {% set listing_count = 0 + listing_nodes|length %}
                  {% for list_node in listing_nodes %}
                    <div class="simple-teaser">{{ drupal_entity('node', list_node.id, 'teaser_horizontal') }}</div>

                    {% if paragraph.field_display_no_ad.value == false and
                      listing_count <= 6 and
                      loop.index == (listing_count - 1) and
                      listing_ad_1 is not empty %}
                      <div class="simple-teaser">{{ listing_ad_1|raw }}</div>
                    {% elseif paragraph.field_display_no_ad.value == false and
                      listing_count > 6 and loop.index == 2 and listing_ad_1 is not empty %}
                      <div class="simple-teaser">{{ listing_ad_1|raw }}</div>
                    {% elseif paragraph.field_display_no_ad.value == false and
                      listing_count > 6 and
                      loop.index == (listing_count - 1) and
                      listing_ad_2 is not empty %}
                      <div class="simple-teaser">{{ listing_ad_2|raw }}</div>
                    {% endif %}
                  {% endfor %}
                {% endif %}
              </div>
            {% endif %}
          {% endif %}
        </div>
        <div class="listing-section-right">
          {% if content.field_dfp_tag.0 is not empty %}
            <div class="listing-ad-wrapper">{% if is_ad_display_allowed('default', 'default') == true %}{{ render_ad(paragraph.field_dfp_tag.target_id)|raw }}{% endif %}</div>
          {% endif %}
          {% if paragraph.field_right_side_content_type.value == 1 and content.field_news_streams is not empty %}
            <div class="listing-news-stream-wrapper">{{ content.field_news_streams }}</div>
          {% elseif paragraph.field_right_side_content_type.value == 2 and content.field_most_viewed_stories is not empty %}
            <div class="listing-most-viewed-stories-wrapper">{{ content.field_most_viewed_stories }}</div>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
{% endblock paragraph %}
